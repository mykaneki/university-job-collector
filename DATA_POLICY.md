# 数据使用政策

## 适用范围

本政策说明本项目运行时产生的 Raw、Simple JSON、metadata、日志和浏览器 profile 的处理边界。它不改变来源网站或第三方内容的权利归属。

本项目的 Apache-2.0 许可证只覆盖本项目原创代码和文档。高校网页、API 响应、招聘正文、图片、姓名、邮箱、电话、商标及其他第三方内容不随代码许可证授权。

## 采集范围

- 只采集无需账号即可访问的招聘信息。
- 登录、验证码、统一身份认证、校园网专属页面和明确受限内容不进入采集范围。
- 采集目的限于按学校、时间和确定性条件整理公开招聘信息。
- 采集器应保持串行、低频访问，并服从来源网站当前的服务条款、robots.txt 和停止要求。

## 本地存储

- `output/` 和 `.diagnostics/` 默认被 Git 忽略，只用于本地运行和排查。
- Raw 可能保留完整 HTML、JSON、图片链接、公开联系方式和页面中的其他个人信息。
- 浏览器 profile 可能包含站点状态。每个 profile 应独立使用，限制本地访问权限，完成排查后按实际需要清理。
- 根据实际用途设置最短留存期限。用途结束、来源撤回或权利人提出有效请求后，应删除不再需要的数据。

## 共享与再分发

- 仓库、Pull Request、issue、讨论区和 Release 不应包含真实 Raw、批量 Simple 输出、Cookie、Token 或浏览器 profile。
- 分享故障证据时，只保留复现所需的最小结构，并替换姓名、电话、邮箱、ID 和其他可识别信息。
- 公开招聘页面可访问不等于获得复制、汇编或再分发授权。发布数据集、搜索服务、镜像或商业产品前，需要单独确认来源网站条款、权利人许可和适用法律。
- Simple JSON 中的 JD 与投递方式仍可能包含受版权保护的正文和个人信息，适用相同限制。

## 安全事件和权利请求

发现仓库或发布物包含凭据、个人信息或受限内容时，先停止继续传播，并通过 [SECURITY.md](SECURITY.md) 指定的私密渠道报告。数据主体、来源网站或其他权利人的删除和更正请求应按具体权利和适用法律处理。

本政策提供项目操作边界，不构成法律意见。部署者负责评估其采集、留存、使用和披露行为。
