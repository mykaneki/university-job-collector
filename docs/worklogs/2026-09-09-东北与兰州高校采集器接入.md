# 2026-09-09 东北与兰州高校采集器接入

## 目标

接入吉林大学、大连理工大学、哈尔滨工业大学、哈尔滨工程大学和兰州大学，并按 `2026-09-06 00:00:00`—`2026-09-08 23:59:59` 实际验证。

## 实际操作

- 吉大固定为 GET HTML 列表与详情，并兼容服务端实际返回的中文数字日期和英文月份日期。
- 大工固定为匿名 JSON API；列表、筛选字典和详情均使用普通 HTTP。
- 哈工大固定为匿名 JSON API；详情页是前端壳，生产直接请求其匿名详情 API。
- 哈工程当前 HTTP 返回 502、HTTPS 在 TLS 握手中断，保存失败 Raw 并明确运行失败。
- 兰大在交互式 Chrome 可浏览，但普通 HTTP 和独立 Playwright 均返回 412；未把用户浏览器状态当作生产依赖。
- 根据独立检查结果，为吉大、大工和哈工大补充详情时间的最终边界校验，并强制大工 `原始链接` 指向校方详情页。

## 结果

- 吉大：5 页，28 条，详情 28/28 成功。
- 大工：10 页，111 条，详情 111/111 成功。
- 哈工大：10 页，152 条，详情 152/152 成功。
- 哈工程：当前网络 502/TLS 阻断，0 条。
- 兰大：HTTP/Playwright 412，0 条；交互式 Chrome 已确认公开列表存在。
- 可用站点合计 291 条 Simple；用于最终结果的 Raw 与 metadata 合计 636 个文件。

## 验证

- 对列表、详情、中英文日期、原始链接和严格六字段输出进行了实际请求与样本核验。
- 独立实采产物：`output/20260909_000115`、`output/20260909_000543`。
- 新增东北批次解析与参数单测；整合全部 46 个 collector 后，全量回归 30/30 通过，全部模块编译与动态导入通过。

## 未完成项

- 哈工程需在可稳定访问学校站点的境内网络重新探索当前平台。
- 兰大需在可被生产任务复用的境内普通出口验证 HTTP，或找到可稳定重放的公开 Cookie/Session 初始化流程。
